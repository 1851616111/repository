ngine.Sync(new(Repository))

	_, err := engine.Insert(&Repository{Repository_name:"123"})
	utee.Chk(err)
	user := new(Repository)

	rows, err := engine.Where("repository_name = ?", 123).Rows(user)
	if err != nil {
	}
	defer rows.Close()
	for rows.Next() {
		err = rows.Scan(user)
		log.Println(user)
		//...
	}

